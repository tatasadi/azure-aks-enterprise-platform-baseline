apiVersion: v1
kind: Service
metadata:
  name: sample-api
  namespace: demo-app
  labels:
    app: sample-api
    environment: dev
    owner: platform-team
spec:
  type: ClusterIP
  selector:
    app: sample-api
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
